/*
element hay HTML element đơn giản chỉ là những thẻ HTML

1. select 1 element
const idElemnet = document.querySelector("name id");
const selectElement = document.querySelector("selector");

2. select 2 or more element
=> return về HTLM colection object
const tagNameElement = document.querySelectorByTagName("tag name");
const classNameElement = document.querySelectorByClass("class name);

=> return về node list object
const selectorAllElement = document.querySelectorAll("selector");
*/
